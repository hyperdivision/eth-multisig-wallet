dist: xenial
language: node_js
node_js:
  - lts/*

addons:
  apt:
    packages:
      - python3.6
      - python3-pip

before_install:
  - python --version
  - pip3 install --upgrade pip
  - pip3 install 'slither-analyzer==0.6.8'

script:
  - slither .
