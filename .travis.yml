dist: xenial
language: node_js
node_js:
  - lts/*

addons:
  apt:
    sources:
      - deadsnakes
    packages:
      - python3.8
      - python3-pip

before_install:
  - pip3 install --upgrade pip
  - pip3 install slither-analyzer

script:
  - slither .
